/* === i18n canonical (generado) === */
export const lang = (
  (typeof window!=="undefined" && (window.IBG_LANG || document.documentElement.getAttribute("lang"))) ||
  "es"
);
try { if (typeof window!=="undefined") window.IBG_LANG = lang; } catch(e){}
export default lang;
